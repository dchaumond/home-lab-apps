# Ansible configuration
[defaults]
inventory = inventories/production/hosts.yml
remote_user = docker-admin
become = false

[privilege_escalation]
become_method = sudo
become_user = root
become_ask_pass = false